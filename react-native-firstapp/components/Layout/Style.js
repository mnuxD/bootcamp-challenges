import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderColor: "red",
    borderWidth: 2,
    marginTop: 20,
  },
  text: {
    fontSize: 20,
  },
});

export default styles;
